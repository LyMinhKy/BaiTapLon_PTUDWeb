@if (TempData.ContainsKey("message"))
{
    BTL_Web.Library.XMessage message = (BTL_Web.Library.XMessage)TempData["message"];
    <div class="alert alert-@message.TypeMsg">
        @message.Msg
    </div>
}

